import React from "react";
import { Typography } from "@material-ui/core";

export const FooterLogo = () => {
  return (
    <Typography variant="h1" style={{ padding: "50px 0px", color: "#ef4123" }}>
      Tonito
    </Typography>
  );
};
